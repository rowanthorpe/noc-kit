@PACKAGE_STRING@
Copyright © 2012, 2013, 2014 Rowan Thorpe.
Report bugs to @PACKAGE_BUGREPORT@

Quick Install Instructions:

 tar xjf @PACKAGE_TARNAME@-@PACKAGE_VERSION@.tar.bz2 && \
  cd @PACKAGE_TARNAME@-@PACKAGE_VERSION@ && \
  mkdir build && \
  cd build && \
  ../configure && \
  make && \
  make install

(or use "tar xJf @PACKAGE_TARNAME@-@PACKAGE_VERSION@.tar.xz" above to extract an xz archive).

See INSTALL file for more details. Note that for test builds the author uses:

 env CFLAGS="-D_POSIX_C_SOURCE=200809L -Wall -Wextra -pedantic-errors \
             -Wno-overlength-strings -static -fomit-frame-pointer -O3 \
             -march=native" ./configure
 make CC=c99
 make install

TODO:

 * add help2man functionality to automake
 * add --pattern to slaughter.sh
 * merge marked tryedit.sh functions into nk_common.sh
